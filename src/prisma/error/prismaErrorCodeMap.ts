import { BbtErrorCode } from "src/error/enum/BbtErrorCode";

export const prismaErrorCodeMap: { [key: string]: { [model: string]: { message: string; code: string } } } = {
	P2025: {
		
	},
	P2016: {
		
	},
	// Add other Prisma error codes here if needed
};
